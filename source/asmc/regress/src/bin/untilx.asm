; v2.22 - Test for pre-existing flag conditions in .UNTILx


	.386
	.model	flat
	.code

	.REPEAT
	.UNTILA
	.REPEAT
	.UNTILAE
	.REPEAT
	.UNTILB
	.REPEAT
	.UNTILBE
	.REPEAT
	.UNTILC
	.REPEAT
	.UNTILE
	.REPEAT
	.UNTILG
	.REPEAT
	.UNTILGE
	.REPEAT
	.UNTILL
	.REPEAT
	.UNTILLE
	.REPEAT
	.UNTILNA
	.REPEAT
	.UNTILNAE
	.REPEAT
	.UNTILNB
	.REPEAT
	.UNTILNBE
	.REPEAT
	.UNTILNC
	.REPEAT
	.UNTILNE
	.REPEAT
	.UNTILNG
	.REPEAT
	.UNTILNGE
	.REPEAT
	.UNTILNL
	.REPEAT
	.UNTILNLE
	.REPEAT
	.UNTILNO
	.REPEAT
	.UNTILNP
	.REPEAT
	.UNTILNS
	.REPEAT
	.UNTILNZ
	.REPEAT
	.UNTILO
	.REPEAT
	.UNTILP
	.REPEAT
	.UNTILPE
	.REPEAT
	.UNTILPO
	.REPEAT
	.UNTILS
	.REPEAT
		.BREAK .IFA
		.BREAK .IFAE
		.BREAK .IFB
		.BREAK .IFBE
		.BREAK .IFC
		.BREAK .IFE
		.BREAK .IFG
		.BREAK .IFGE
		.BREAK .IFL
		.BREAK .IFLE
		.BREAK .IFNA
		.BREAK .IFNAE
		.BREAK .IFNB
		.BREAK .IFNBE
		.BREAK .IFNC
		.BREAK .IFNE
		.BREAK .IFNG
		.BREAK .IFNGE
		.BREAK .IFNL
		.BREAK .IFNLE
		.BREAK .IFNO
		.BREAK .IFNP
		.BREAK .IFNS
		.BREAK .IFNZ
		.BREAK .IFO
		.BREAK .IFP
		.BREAK .IFPE
		.BREAK .IFPO
		.BREAK .IFS
		.BREAK .IFZ

		.CONTINUE .IFA
		.CONTINUE .IFAE
		.CONTINUE .IFB
		.CONTINUE .IFBE
		.CONTINUE .IFC
		.CONTINUE .IFE
		.CONTINUE .IFG
		.CONTINUE .IFGE
		.CONTINUE .IFL
		.CONTINUE .IFLE
		.CONTINUE .IFNA
		.CONTINUE .IFNAE
		.CONTINUE .IFNB
		.CONTINUE .IFNBE
		.CONTINUE .IFNC
		.CONTINUE .IFNE
		.CONTINUE .IFNG
		.CONTINUE .IFNGE
		.CONTINUE .IFNL
		.CONTINUE .IFNLE
		.CONTINUE .IFNO
		.CONTINUE .IFNP
		.CONTINUE .IFNS
		.CONTINUE .IFNZ
		.CONTINUE .IFO
		.CONTINUE .IFP
		.CONTINUE .IFPE
		.CONTINUE .IFPO
		.CONTINUE .IFS
		.CONTINUE .IFZ
	.UNTILZ

	END
