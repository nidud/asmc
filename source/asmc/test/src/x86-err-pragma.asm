    .code

.pragma list(push, 1)
.pragma list(push, 0)
.pragma list(pop)
.pragma list(pop)
.pragma list(pop)

.pragma list(pop))

.pragma(list(pop, 1))
.pragma(list(push, 2))

.pragma(cref(pop, 1))
.pragma(cref(push, 2))

    end
